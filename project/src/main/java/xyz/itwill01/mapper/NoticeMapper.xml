<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="xyz.itwill01.mapper.NoticeMapper">
	<insert id="insertNotice">
		<selectKey resultType="int" keyProperty="noticeNo" order="BEFORE">
			select notice_seq.nextval from dual
		</selectKey>
		insert into notice values(#{noticeNo}, #{noticeTitle}, #{noticeContent}
			, #{noticeImage}, sysdate, #{noticeUpdate}, 0, #{noticeStatus})
	</insert>
	
	<delete id="deleteNotice">
		delete from notice where noticeNo=#{noticeNo}
	</delete>
	
	<select id="selectNotice">
		select noticeNo, noticeTitle, noticeContent, noticeImage, noticeRegdate
			, noticeUpdate, noticeCount, noticeStatus from notice where noticeNo=#{noticeNo}
	</select>
	
	<select id="selectNoticeCount" resultType="int">
		select count(*) from notice
	</select>
	
	<select id="selectNoticeList" resultType="Notice">
		select noticeNo, noticeTitle, noticeContent, noticeImage, noticeRegdate
			, noticeUpdate, noticeCount, noticeStatus from notice order by noticeNo desc
	</select>
</mapper>